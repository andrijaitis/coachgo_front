//good test

import { ColorthemeDirective } from './colortheme.directive';

describe('ColorthemeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorthemeDirective();
    expect(directive).toBeTruthy();
  });
});
